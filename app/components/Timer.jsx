'use strict';

var React = require('react');

var Timer = React.createClass({
    render: function renderTimer() {
        return (
            <div>Timer.jsx</div>
        );
    }
});

module.exports = Timer;